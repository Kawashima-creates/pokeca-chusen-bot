# ローカルのdry-run等で生成される状態ファイルはコミットしない。
# CI(GitHub Actions)側は `git add -f state.json` で強制追加し永続化する。
state.json

__pycache__/
*.pyc
.env
.venv/
venv/

# ローカル常駐Botのログ
*.log

# 申込状態（ローカル）
applied.json

# 当選メールチェックの状態（CIが-fで永続化）
mail_state.json

# Bot死活監視ファイル
bot_heartbeat
watchdog.log
